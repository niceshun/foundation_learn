# name = "jjjjjj" \
#        "jjjjojoo" \
#        "kjjjojo"   # 跳行的示例

print(5 % 3)  # 求模运算符%表示两数相除返回余数

"""# 在while循环中，设置标志是很有用的做法
name = "who are you?"
flag = True
while flag:
    whose_name = input(name)
    if whose_name == "aaa":
        flag = False
    else:
        print(whose_name)"""

# break也可以实现跳出循环
# contine可重新回到循环起点

"""nums = [1, 1, 1, 2, 3]
num = 1
while num in nums:
    nums.remove(num)
print(nums)"""

"""nums = {}
flag = True
while flag: 
    name = input("what is your name?")
    like = input("what is your like?")
    nums[name] = like  # 重复定义会覆盖掉原先有的值
    somebody = input('anyone else?(yes/no)')
    if somebody == "no":
        flag = False
for name, like in nums.items():
    print(f'{name} like {like}!')"""






























